
//********************** Asynchronous functions *******************************************

function getData(){
    return new Promise((resolve,reject)=> {setTimeout(()=> {resolve("Data success")},2000)});
}

// here iam preparing the asyn operation that is resolve and reject
//*****************************   then and catch functions *********************************
 getData().then((data)=> {console.log(data);});
 //here we are using then function. this function only allows the data if the output is resolve from the above function
 getData().catch((error)=>{console.log(error);});
 // here we are using catch function. this function allows only the data if the output is rejected from the above function
 
 // ***in simple words it works like try and catch blocks in c, java etc...

 //******************************* setTimeout() function************************** 
setTimeout(() => {
     console.log("Data received");
 }, 5000);
 //this setTimeout() function is used to set the time to get the output in mseconds
 
//********************************** setInterval() function********************** 
//setInterval(() => {
//     console.log("Checking for new data...");
 //}, 3000);
 //same as setTimeout() function it will repete again and again at particular time interval



 //********************************** fetching API and modifying it*******************
 fetch("https://jsonplaceholder.typicode.com/todos")
.then(data=>data.json())
.then(data=>data.filter(d=>d.id%2===0))
.then(data=>data.map(d=>d.title="manaswi" + d.title ))
.then(data=>{console.log(data);})
.catch(data=>{console.error(data);});

 //-> here fetch function is going to send the request to the API and we will get the resolve/reject
 //-> by using this we are doing the API call to get todo data
 //-> here the first then is converting the fetched data into json 
 //-> the second then is filtering the id 
 //-> the third then is modifying the title by adding "manaswi" in front of the title by using map function
 //-> the fourth then is going to pront the data

 